name: Order API CI

on:
  push:
    branches:
      - main
      - stage

  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  call-reusable-workflow:
    uses: abdosaad203/reusable-backend-ci/.github/workflows/backend-ci.yml@main

    with:
      dotnet-version: '10.0.x'
      project-path: 'Order.API/Order.API.csproj'
      test-path: 'Order.API.Tests/Order.API.Tests.csproj'
      run-tests: true
      image-name: 'order-api'